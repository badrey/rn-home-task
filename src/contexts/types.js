/* @flow */
export type Theme = {
    headerColor: string,
    centerSectionColor: string,
    rightSectionColor: string,
    leftSectionFirstSubsectionColor: string,
    leftSectionSecondSubsectionColor: string,
    footerColor: string,
};

export type ThemeProps = {
    theme: Theme,
};
