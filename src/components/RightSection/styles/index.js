/* @flow */
import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#F5D26A",
        height: "60%",
        margin: 4,
        minWidth: "15%",
        maxWidth: "100%",
    },
});
