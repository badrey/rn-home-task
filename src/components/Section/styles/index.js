/* @flow */
import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: "30%",
        padding: 4,
        width: "100%",
    },
    subContainer: {
        flex: 1,
        backgroundColor: "#CCC86F",
    },
});
